# Configuration Directory

This directory is used for configuration files for the simulation_world package.

*Note: This directory is intentionally kept in the repository even when empty to satisfy build requirements.*
